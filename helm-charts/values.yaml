replicaCount: 1

image:
  repository: asia-south1-docker.pkg.dev/automation-480006/my-repo/auth-service
  tag: "24bf75fda421c72c84aa648ec9353a7ad9143dfb"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
