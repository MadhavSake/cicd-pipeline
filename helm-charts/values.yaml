replicaCount: 1

image:
  repository: asia-south1-docker.pkg.dev/automation-480006/my-repo/auth-service
  tag: "c22b164636aceebd1e2ea43368a766952f8609a7"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
