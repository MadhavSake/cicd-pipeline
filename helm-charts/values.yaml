replicaCount: 2

image:
  repository: us-central1-docker.pkg.dev/automation-480006/my-repo/auth-service
  tag: "d79c111da33d77041078eb21b21d678522db8aba"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
