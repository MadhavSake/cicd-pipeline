replicaCount: 1

image:
  repository: asia-south1-docker.pkg.dev/automation-480006/gke-docker-repo/auth-service
  tag: "047fc4656528cda867e2f0858667a9926c9b03f1"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
