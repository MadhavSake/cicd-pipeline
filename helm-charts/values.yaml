replicaCount: 1

image:
  repository: asia-south1-docker.pkg.dev/automation-480006/my-repo/auth-service
  tag: "b86f3960e1ce5086b57053152b0c8d1e33b0ca1f"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
