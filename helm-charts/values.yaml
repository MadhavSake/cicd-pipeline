replicaCount: 1

image:
  repository: us-central1-docker.pkg.dev/automation-480006/my-repo/auth-service
  tag: "dcbdd190e712101f462a7d988db42995cdfa0014"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

resources:
  limits:
    cpu: "200m"
    memory: "256Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70
